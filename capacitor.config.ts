import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.projectmangaer.app',
  appName: 'Project Manager',
  webDir: 'build'
};

export default config;
